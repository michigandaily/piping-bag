import { describe, it } from "node:test";

describe("pipe upload", async () => {
  it("parses pipe.config.js correctly", async () => {});
  it("parses pipe.config.json correctly", async () => {});
  it("uses default pipe configurations on optional fields", async () => {});
});

describe("pipe schedule", async () => {
  it("parses pipe.config.js correctly", async () => {});
  it("parses pipe.config.json correctly", async () => {});
  it("uses default pipe configurations on optional fields", async () => {});
});

describe("pipe deploy", async () => {
  it("parses pipe.config.js correctly", async () => {});
  it("parses pipe.config.json correctly", async () => {});
  it("uses default pipe configurations on optional fields", async () => {});
});

describe("pipe list", async () => {
  it("parses pipe.config.js correctly", async () => {});
  it("parses pipe.config.json correctly", async () => {});
  it("uses default pipe configurations on optional fields", async () => {});
});

describe("pipe destroy", async () => {
  it("parses pipe.config.js correctly", async () => {});
  it("parses pipe.config.json correctly", async () => {});
  it("uses default pipe configurations on optional fields", async () => {});
});
